#include <bits/stdc++.h>
using namespace std;

int main() {
    int N, M, Q;
    cin >> N >> M >> Q;
    vector<int> p_a, p_b;  
    for (int i = 0; i < N; i++) {
        cin >> p_a[i];
    }
    cout << p_a[0];

    return 0;
}